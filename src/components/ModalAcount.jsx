import Modal from "./Modal";

export default function ModalAcount({title,root}){
    return(
        <Modal title={title} root={root} >
            Modal de configuración de cuenta
        </Modal>
    )
}